---

- name: start rpcbind service
  service:
    name: rpcbind
    state: started
    enabled: yes

- name: install autofs
  kewlfft.aur.aur:
    name: autofs
    state: present 
    skip_pgp_check: yes
  become: yes
  become_user: aur_builder