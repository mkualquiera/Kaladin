---

- name: start rpcbind service
  service:
    name: rpcbind
    state: started
    enabled: yes

- name: install autofs
  kewlfft.aur.aur:
    name: autofs
    state: present 
  become: yes
  become_user: aur_builder